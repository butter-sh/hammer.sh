name: "{{project_name}}"
version: "2.0.0"
description: "SVG to icon set generator using CSS mask-image"
author: ""
license: "MIT"

# Dependencies from other arty.sh repositories
references:
  # Add any dependencies here

# Entry point script
main: "icony.sh"

# Available commands/scripts
scripts:
  generate: "bash icony.sh generate"
  serve: "bash icony.sh serve"
  clean: "bash icony.sh clean"
  init: "bash icony.sh init"
